#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run typecheck
echo "Running type checking..."
npm run typecheck || { echo "Type checking failed, commit aborted!"; exit 1; }

# Run lint-staged for formatting and linting
echo "Running lint-staged..."
npx lint-staged
