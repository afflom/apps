# Environment variables for TypeScript PWA Template
# Copy this file to .env and add your values for local development

# GitHub token for deployment and GitHub operations
# Required for the deploy:dev script and some GitHub CLI operations
# Generate a token at: https://github.com/settings/tokens
# Required scopes: workflow, repo
GITHUB_TOKEN=

# Anthropic API key for Claude Code integration
# Required for Claude Code CLI and VS Code extension
# Generate a key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Application configuration
# Replace these values with appropriate values for your deployment

# Application title shown in the UI (required)
APP_TITLE=

# Node environment: development, test, or production (required)
# - development: for local development with hot reloading
# - test: for running tests
# - production: for production deployment with optimizations
NODE_ENV=