# GitHub Token for deploying to GitHub Pages and testing workflows
# Create a token with repo and workflow permissions at https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Node environment (development, test, production)
NODE_ENV=development

# Base URL for production (empty for root, otherwise with trailing slash)
BASE_URL=/

# Anthropic API Key for Claude Code (optional)
ANTHROPIC_API_KEY=your_anthropic_api_key_here